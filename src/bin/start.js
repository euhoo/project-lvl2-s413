#!/usr/bin/env node

/* я должен на вход принять 2 строки для сравненияЖ 1 из файла до и 2 из пайла после
здесь наверняка должен быть только исполняющий механизм,который экспортирует функцию по умолчанию.
для начала реализую функцию здесь


*/
export default start = (str1,str2) => {
    
}